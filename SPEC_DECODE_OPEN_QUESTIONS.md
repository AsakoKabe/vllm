# Открытые вопросы и готовность к Фазе 1

> Канонический список открытых вопросов проекта spec-decode timing.
> Основной план: `SPEC_DECODE_TIMING_PLAN.md`. Дата: 2026-06-25.

> **⚠️ Scope-решение (2026-07-02): оставляем только метрики, которые считаются напрямую из кода.**
> Tier 3 (декомпозиция $T_{SD}=T^*_{id}+\Delta_{part}+\Delta_{rej}$, фит $\alpha_k,\beta_k$, ideal-draft, Bellman, trace sink)
> **исключён из scope** — эти величины требуют offline-фита/эмулятора/допущений. Поэтому вопросы
> **A1, A2, A3, A4, C9, C11 более неактуальны** (они гейтили только Tier 3). B5–B7, C8, D1–D3 уже закрыты в реализации.

## TL;DR — нужны ли ответы для Фазы 1?

**Нет.** Фаза 1 — изолированный каркас (флаг + `SpecDecodeTimer` + `SpecDecodeTimingStats` +
расширение `SpecDecodingStats`/`Prom`/`Logging`); не трогает горячий путь, Tier 2 и Tier 3.
Из всех вопросов Фазу 1 касается только **C8** (тип метрики для латентностей), и у него есть
безопасный обратимый дефолт (целые микросекунды, `Counter→Vector`, как у `num_accepted_tokens_per_pos`).
Всё остальное блокирует Фазы 2–7, а не Фазу 1.

→ **Фазу 1 можно начинать сейчас.** Ответы автора нужны к Tier 3 (A1–A4, C9, C11); B5–B7 я закрою
чтением кода к Фазам 2–3.

## Что нужно для Фазы 1 (deliverables)

1. Флаг `ObservabilityConfig.spec_decode_timing: bool = False` (+ CLI/env), `config/observability.py:56`.
2. `vllm/v1/spec_decode/timing.py`: `SpecDecodeTimer` (пул CUDA-событий, `time_stage`, `drain_previous`,
   ping-pong глубины 2) + `SpecDecodeTimingStats` (поля-носители; внутри float ms).
3. Расширить `SpecDecodingStats` (timing-поля + `observe_timing()`), `SpecDecodingLogging`
   (`observe`/`log`), `SpecDecodingProm` (экспорт-метрики) в `vllm/v1/spec_decode/metrics.py`.
4. Юнит-тесты агрегации (без GPU): `observe_timing`, формирование `Vector`.

Зависимость от открытых вопросов: только **C8** (дефолт ниже). Остальные deliverables самодостаточны —
горячий путь не инструментируется, метрики на этом этапе пустые.

## Дефолты, которые беру для Фазы 1 без ожидания ответов

- **C8 (тип метрики):** целые **микросекунды** в `Counter`; per-position/per-K → `Vector[int]`
  (зеркало `num_accepted_tokens_per_pos`). Внутри `SpecDecodeTimingStats` держу float ms, конверсия
  в µs — только на экспорте. Полностью обратимо (можно сменить на Histogram/float-Vector позже).
- **D1 (double-buffer flush):** на shutdown дренажируем последний шаг; потеря ≤1 шага допустима.
- **D3 (diffusion-ветка):** зеркалю существующий паттерн (отдельные имена либо пропуск), как уже
  сделано для acceptance в `metrics.py`.

## Полный список открытых вопросов

| ID | Вопрос | Уровень | Блокирует Фазу 1? | Дефолт / рекомендация | Кто отвечает |
|----|--------|---------|-------------------|------------------------|--------------|
| A1 | Эмулятор ideal-draft `T_id`: реальный rerun с «читерским» proposer (ground-truth K токенов) vs аналитический пересчёт из cost-model | Tier 3 | Нет | — | ты (автор) |
| A2 | Калибровка `α_k,β_k`: выделенный свип по фикс. k vs бины боевого прогона | Tier 3 | Нет | свип (R²-выброс k=5 намекает) | ты |
| A3 | `T_AR` база: отдельный vanilla-AR прогон vs вывод из `T_T(0)` | Tier 3 | Нет | — | ты |
| A4 | B=1 для декомпозиции (`max_num_seqs=1`)? | Tier 3 | Нет | да (как в статье) | ты (подтвердить) |
| B5 | Входит ли `compute_logits` (`:4367`) в `T_T(K)` | Фаза 2 | Нет | да (логиты K+1 позиций) | код (я) |
| B6 | Границы `T_reject` (только `rejection_sampler` `:3605`?) | Фаза 2 | Нет | голый `rejection_sampler` | код (я) |
| B7 | EAGLE-3 draft-путь: per-position mapping, искажение position-0 | Фаза 3 | Нет | — | код (я) |
| C8 | Тип метрики латентностей (µs-int / Histogram / float-Vector) | **Фаза 1/4** | **Да — но есть дефолт** | µs-int `Counter→Vector` | ты (опц.) / дефолт |
| C9 | Объём per-position routing-трейса (полный `routing_data` / bitset / prefix-union) | Tier 3 | Нет | bitset экспертов на позицию | ты + я |
| C10 | Вывод оффлайн-харнесса: конструировать `Metric` напрямую vs Prometheus registry | Tier 3 | Нет | конструировать `Metric` (dataclass из `reader.py`) | дефолт |
| C11 | Критерий валидации: воспроизвести Table 1 с какой толерантностью | приёмка | Нет | ±N% (задать) | ты |
| D1 | Flush double-buffer на shutdown | Фаза 1 | Нет (дефолт) | flush, потеря ≤1 шаг | дефолт |
| D2 | Логический vs физический id экспертов (EP>1 / EPLB) | Tier 2 | Нет (пилот EP=1) | logical при EP>1 | код (я) |
| D3 | diffusion-ветка в `Prom`/`Logging` | Фаза 1/4 | Нет (дефолт) | зеркало acceptance | дефолт |

## Карта «фаза → блокирующие вопросы»

| Фаза (см. план §8) | Блокеры |
|---|---|
| **1. Каркас** | — (только C8, дефолт есть) → **готово к старту** |
| 2. Tier 1: target/verify/sample/draft-coarse + тэг | B5, B6 |
| 3. Tier 1 fine: per-position draft | B7 |
| 4. Транспорт + слияние + экспорт | C8, D3 |
| 5. Tier 2: `Ū_r` | D2 |
| 6. Tier 3a: per-round трейс-сток | A4, C9 |
| 7. Tier 3b: оффлайн-декомпозиция (Bellman/ideal-rerun) | A1, A2, A3, C10 |
| Приёмка | C11 |

## Что разблокирует ответ автора (приоритет к Tier 3)
Чтобы Фазы 6–7 стали полностью исполнимыми, к моменту Tier 3 нужны: **A1** (механизм `T_id`),
**A2** (калибровка), **A3** (`T_AR`), **A4** (B=1), **C9** (формат трейса), **C11** (толерантность).
B5–B7 и D2 закрываются чтением кода и не требуют твоего участия.
